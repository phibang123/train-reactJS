// handleButtonClick = (ghe) => console.log(ghe)

//     renderColumn = (arr) => arr.map((row, i)=> (<button onClick={() => this.handleButtonClick(row)} className="d-flex justify-content-center align-items-center" key={i} style={{color:'white', background: 'pink', width:50 , height: 50}}>{`${row?.soGhe}`}</button>)
//     )
    
//     renderGhe = () => this.props.datVeXemPhimReducer.map((ghe, index) => index !==  0 &&
//         <div className="col-12" key={index}>
//             <div className="m-2 d-flex">
//                 {this.renderColumn(ghe?.danhSachGhe)} 
//             </div>
//         </div>
//     )
        